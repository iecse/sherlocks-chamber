/*
A lobby where teams wait before the match starts.
Contains:
-WebSocket connection setup to sync with the server.
-visual rep of teams in the match.
-Countdown timer for match start.
-Start match button (for the admin).
-Game rules reminder.
-also has link to leaderboard page

*/

function Lobby() {
    return (
        <div>Lobby</div>
    )
}

export default Lobby